# 软路由云端自动化编译与日志设计复盘指南

本项目（如 [immortalwrt-Quake](https://github.com/wuying99130/immortalwrt-Quake)）针对特定单板硬件（如 NanoPi R1S-H3）实现了云端自动化编译。以下是核心架构技巧与日志呈现规范，供后续项目复刻参考。

---

## 一、 架构与工作流设计技巧（GitHub Actions）

1. **工作流模块化与步骤解耦**
   * **实现方式**：将编译流程拆分为独立阶段（检出源码、初始化依赖、更新 Feeds、应用 DTS 补丁、应用配置、执行编译、上传产物）。
   * **复刻要点**：在 `.github/workflows/` 的 YAML 文件中为每个步骤设置清晰的 `name`，让流水线时间轴一目了然。

2. **硬件差异化适配（自动化注入 DTS 补丁）**
   * **实现方式**：通过独立脚本（如 `patch-dts.sh`）在编译前自动将设备树文件注入到内核源码树。
   * **复刻要点**：采用“补丁脚本 + 自动化执行”替代手动修改，确保每次云端全新编译都能正确应用硬件描述。

3. **预配置固化（`.config` 种子文件）**
   * **实现方式**：将插件、驱动和系统配置固化在 `.config` 文件中。
   * **复刻要点**：避免交互式菜单勾选，确保每次构建结果的高度一致。

---

## 二、 日志呈现与可读性优化技巧（Log UX）

1. **视觉分隔与区块化**
   * **实现方式**：使用显眼的边框字符（如 `=== ... ===`）包裹关键输出。
   * **复刻要点**：在 Shell 脚本中使用 `echo "========================================="` 划分视觉重点。

2. **状态图标与时间戳注入**
   * **实现方式**：输出中嵌入进度符号（如 `◔`、`*`）与时间前缀（如 `[00:29:01]`）。
   * **复刻要点**：便于在海量流水线日志中精准定位耗时环节。

3. **错误与警告的前置预检**
   * **实现方式**：在执行长时间核心编译前加入预检步骤。
   * **复刻要点**：提前暴露 Kconfig 递归依赖或配置冲突，避免白白浪费几小时的编译时间。

---

## 三、 产物管理与交付技巧

1. **多格式产物同时打包**：同时输出 `ext4-sdcard`（支持动态扩容）和 `squashfs-sdcard`（防掉电损坏）镜像。
2. **版本与信息溯源**：随固件生成 `build-info` 文件，记录分支、提交哈希和时间戳，确保刷机后可精准追溯。
